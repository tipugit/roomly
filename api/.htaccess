<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /api/

  # Route /api/* to index.php when not a real file
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [L,QSA]
</IfModule>

<Files "config.php">
  Require all denied
</Files>
