<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # API — leave PHP files alone
  RewriteRule ^api/ - [L]

  # Social share previews (Open Graph) for /s/{token}
  RewriteRule ^s/([a-zA-Z0-9]{4,32})$ share.php?token=$1 [L,QSA]

  # Serve existing files and directories as-is
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # SPA fallback — send all other requests to index.html
  RewriteRule ^ index.html [L]
</IfModule>

# Cache static assets (optional, safe for shared hosting)
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

# Enable gzip compression when available
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/css application/javascript application/json image/svg+xml
</IfModule>
